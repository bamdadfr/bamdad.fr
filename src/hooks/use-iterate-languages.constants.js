/**
 * @name useIterateLanguagesConstants
 * @description constants feeding the `useIterateLanguages` hook
 * @type {Object.<String, Number>}
 */
export const useIterateLanguagesConstants = {
    'hide': 2400,
    'iterate': 1200,
    'show': 0,
}